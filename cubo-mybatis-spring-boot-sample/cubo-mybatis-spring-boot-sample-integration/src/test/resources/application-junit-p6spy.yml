server:
  port: 9091

spring:
  datasource:
    url: jdbc:p6spy:mysql://mysql.server:3306/mybatis_spring_boot_sample_integration?useUnicode=true&characterEncoding=utf8&useSSL=false&allowMultiQueries=true&serverTimezone=GMT%2b8
    username: root
    password: rootpassword
    driver-class-name: com.p6spy.engine.spy.P6SpyDriver

zeka-stack:
  logging:
    level:
      project: warn
      root: debug
  mybatis:
    enable-illegal-sql-interceptor: false


